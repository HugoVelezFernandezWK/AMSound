<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/logo_amsound"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_amsound"
        android:supportsRtl="true"
        android:theme="@style/Theme.AMSound"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".Voz"
            android:exported="false" />
        <activity
            android:name=".PanelUsuario"
            android:exported="false" />
        <activity
            android:name=".ui.usuario.PanelUsuarioFragment"
            android:exported="false" />
        <activity
            android:name=".PiezaMusical"
            android:exported="false" />
        <activity
            android:name=".Agrupacion"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AMSound.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>